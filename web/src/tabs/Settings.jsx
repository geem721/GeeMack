import TabPlaceholder from "./TabPlaceholder.jsx";

// Not yet assigned its own migration phase in MIGRATION_PLAN.md — likely to accrete
// settings incrementally as each feature phase lands rather than as one big-bang phase.
export default function Settings() {
  return <TabPlaceholder name="Settings" phase="a future phase (not yet scoped)" />;
}
